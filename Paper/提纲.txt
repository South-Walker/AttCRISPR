而在我们的工作中，不需要其余的事后解释技术，只需要分析空间注意力模块产生的first-order preference即可（不使用时间注意力模块是因为时间域AttCRISPR产生的first-order preference是向量形式的，这会给我们的理解带来困难）。具体而言，我们将数据集中的每个sgRNA输入到空间域的AttCRISPR中，为了获取空间注意力模块的输出并取均值，结果经过ZScore正则化后，得到最终的first-order preference矩阵，如图8所示
图8中结果的结果与wang等人基本一致，都捕捉到了。。。。。。，这在一定程度上也说明了在sgRNA活性预测问题上，注意力机制可以给模型带来可解释性。


3.3.2
时间注意力机制带来的局部可解释性
一阶



讨论
同样使用了手制特征进行增强，AttCRISPR增强的幅度没有DeepHF大（写数值），这可能是因为